__version__ = "0.1"

# Available at setup time due to pyproject.toml
from setuptools import setup

setup(
    name="phaselocker",
    version=__version__,
    packages=["phaselocker"],
    install_requires=[],
)
